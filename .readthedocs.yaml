version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    rust: "1.91"
  commands:
    # Run the build script with error handling and security checks
    - ./scripts/build-docs.sh
  pull_requests:
    enabled: true
python:
  install:
    - method: pip
      path: .
